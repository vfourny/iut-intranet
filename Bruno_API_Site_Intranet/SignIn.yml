info:
  name: SignIn
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/trpc/auth.signInWithPassword
  body:
    type: json
    data: |-
      {
          "email": "armand@example.com",
          "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: SignUp
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/trpc/auth.signUpWithPassword
  body:
    type: json
    data: |-
      {
          "name": "Armand Cuvelier",
          "email": "armand@example.com",
          "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

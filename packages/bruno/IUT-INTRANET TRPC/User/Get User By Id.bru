meta {
  name: Get User By Id
  type: http
  seq: 2
}

get {
  url: {{SERVER_URL}}/{{TRPC}}/user.getById?input={"userId":"{{activeUserId}}"}
  body: none
  auth: inherit
}

params:query {
  input: {"userId":"{{activeUserId}}"}
}

settings {
  encodeUrl: true
  timeout: 0
}

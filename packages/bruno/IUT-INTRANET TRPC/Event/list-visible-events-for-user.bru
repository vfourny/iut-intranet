meta {
  name: List Visible Events For User
  type: http
  seq: 3
}

get {
  url: {{SERVER_URL}}/{{TRPC}}/event.listVisibleForUser?input={}
  body: none
  auth: inherit
}

params:query {
  input: {}
}

vars:pre-request {
  TRPC: trpc
  SERVER_URL: http://localhost:8000
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: Sign Out
  type: http
  seq: 5
}

post {
  url: {{SERVER_URL}}/{{TRPC}}/auth.signOut
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

vars:pre-request {
  SERVER_URL: http://localhost:8000
  TRPC: trpc
}

settings {
  encodeUrl: true
  timeout: 0
}
